# Schema

This folder contains simple schema definitions for the Virtual-Machine project.

## Files

- `schema.sql` — SQL table definitions for virtual machines, users, and activity logs.
- `virtual_machine.schema.json` — JSON Schema for validating a virtual machine object.
- `user.schema.json` — JSON Schema for validating a user object.

These files are starter templates and can be expanded as the project grows.
